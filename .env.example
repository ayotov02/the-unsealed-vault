# ── API ───────────────────────────────────────────────────
# Leave empty in development (Vite proxy handles /api → localhost:3001).
# Set to the full backend URL in production (e.g. https://api.epsteingpt.com).
VITE_API_URL=
